const hotData = require('./hotels');
const revData = require('./reviews');
const userData = require('./users');
const comData = require('./comments');

module.exports = {
  hotels: hotData,
  reviews: revData,
  users: userData,
  comments: comData
};